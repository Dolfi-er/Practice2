# Microservices E-commerce API

Проект демонстрирует архитектуру микросервисов для системы управления заказами, построенной на Node.js с использованием Express, Docker и API Gateway.

## 🏗️ Архитектура

Проект состоит из трех основных сервисов:

### **API Gateway** (`api_gateway/`)
- Единая точка входа для всех клиентских запросов
- Маршрутизация запросов к соответствующим сервисам
- Аутентификация и авторизация
- Rate limiting и логирование
- Проксирование запросов

### **Users Service** (`service_users/`)
- Управление пользователями (регистрация, аутентификация, профили)
- JWT-токены для аутентификации
- Ролевая модель (пользователи/администраторы)
- Хэширование паролей с использованием bcrypt

### **Orders Service** (`service_orders/`)
- Создание и управление заказами
- Статусы заказов (created, in_progress, completed, cancelled)
- Валидация бизнес-правил
- Пагинация и фильтрация заказов

## 📁 Структура проекта
.
├── api_gateway/ # API Gateway сервис  
│ ├── Dockerfile  
│ ├── index.js  
│ └── package.json  
├── service_users/ # Сервис пользователей  
│ ├── Dockerfile  
│ ├── index.js  
│ └── package.json  
├── service_orders/ # Сервис заказов  
│ ├── Dockerfile  
│ ├── index.js  
│ └── package.json  
├── docs/ # Документация  
│ └── openapi.yaml # OpenAPI спецификация  
├── tests/ # Тесты  
│ └── test_api.sh # Скрипт для тестирования API  
├── docker-compose.yml # Docker Compose конфигурация  
└── README.md # Документация проекта  


## 🚀 Быстрый старт

### Предварительные требования

- [Docker](https://docs.docker.com/get-docker/) и [Docker Compose](https://docs.docker.com/compose/install/)
- Node.js 16+ (для локальной разработки)

### Запуск с помощью Docker Compose

#### Клонирование репозитория
git clone <repository-url>
cd microservices-ecommerce

#### Сборка и запуск контейнеров
docker-compose up --build

#### Сервисы будут доступны по адресам:
 API Gateway: http://localhost:8000  
 Users Service: http://localhost:8001  
 Orders Service: http://localhost:8002  

